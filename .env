# 默认数据模式。
# 开发环境（vite dev / vitest）默认 mock：数据保存在浏览器 localStorage，不依赖后端。
VITE_DEFAULT_API_MODE=mock

# Vite 开发服务器的 /api 代理目标。本机后端默认监听 8080。
# 通过 SSH 将服务器 18081 映射到本机时，可在 .env.local 中覆盖为 http://localhost:18081。
VITE_DEV_PROXY_TARGET=http://localhost:8080
