interface:
  display_name: "QTA Development Orchestration"
  short_description: "Coordinate bounded QTA roles, evidence, and stage gates"
  default_prompt: "Use $qta-development-orchestration as the parent controller for this bounded QTA delivery lifecycle."
policy:
  allow_implicit_invocation: true
