interface:
  display_name: "QTA Task Contract"
  short_description: "Freeze QTA scope, acceptance criteria, and evidence"
  default_prompt: "Use $qta-task-contract to create a bounded task contract before implementation begins."
policy:
  allow_implicit_invocation: true
