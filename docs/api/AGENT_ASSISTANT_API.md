# Agent Assistant API (Pointer Stub)

> **状态**：前端仓库 pointer stub，用于让 QTA AI 治理 skill 引用路径解析。
>
> **权威来源（authoritative source）**：后端控制仓库 `docs/api/AGENT_ASSISTANT_API.md`。
>
> OpenClaw Agent API 契约（9 个只读 GET 端点 `/api/v1/agent/**`、Bearer 鉴权、限流、统一审计、
> TrustedAnswer、OpenClaw Tool Plugin）的完整定义、请求/响应示例与安全边界均在后端控制仓库维护，
> 本仓库不复制。前端需要联调 OpenClaw / QQ 远程助手时，以后端控制仓库文档与
> `docs/features/OPENCLAW_AGENT_ASSISTANT_DESIGN.md` 为准。
>
> 仅当明确属于 OpenClaw / QQ Agent API 场景时，才在父协调器启用 `qta-openclaw-integration` skill。
